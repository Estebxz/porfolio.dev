---
interface Props {
  class?: string;
  id: string;
  ariaLabel: string;
}

const { class: className, id, ariaLabel } = Astro.props;
---

<section
  id={id}
  data-section={id}
  aria-label={ariaLabel}
  class:list={[
    className,
    "section",
    "scroll-m-20",
    "mx-auto",
    "w-full",
    "max-w-screen-2xl",
    "px-4",
    "sm:px-6",
    "md:px-10",
    "lg:px-24",
    "xl:px-36",
  ]}
>
  <slot />
</section>
